services:
  # Your Python Application
  app:
    build: .
    container_name: Riyagpt-app
    ports:
      - "8000:8000"
    volumes:
      - .:/app
      - /app/Riyagpt
    environment:
      - OLLAMA_HOST=http://host.docker.internal:11434
    extra_hosts:
      - "host.docker.internal:host-gateway"
